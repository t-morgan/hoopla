[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
addopts = -q --import-mode=importlib
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests requiring actual ML models or API calls
    unit: marks tests as fast unit tests
